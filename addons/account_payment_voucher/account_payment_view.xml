<?xml version="1.0" encoding="utf-8"?>
<openerp>
    <data>
        <record id="view_payment_order_form" model="ir.ui.view">
            <field name="name">payment.order.form</field>
            <field name="model">payment.order</field>
            <field name="inherit_id" ref="account_payment.view_payment_order_form"/>
            <field name="arch" type="xml">
                <button position="after">
                    <button type="object" states="done" string="Cancel Payments and set to Draft" name="action_cancel_payments"/>
                    <!-- <button name="create_voucher" string="Create Vouchers" type="object"/> -->
                    <!-- <attribute name="attrs">{'invisible':['|',('state','=','done'),('type_with_advance_payment','=',True)]}</attribute> -->
                </button>
                <div class=" oe_right oe_button_box">
                    <button class="oe_inline oe_stat_button" states="done" type="action" name="%(account_voucher.action_vendor_payment)d" 
                        icon="fa-strikethrough" context="{'search_default_payment_order_id':active_id}">
                        <field name="voucher_count" widget="statinfo"/>
                    </button>
                </div>
                <xpath expr="//form//field[@name='move_line_id']" position='after'>
                    <field name="voucher_id"/>
                </xpath>
<!--                 <tree position="replace">
                    <tree string="Payment Line">
                        <field name="ml_inv_ref" />
                        <field name="partner_id"/>
                        <field name="communication"/>
                        <field name="bank_id" domain="[('partner_id', '=', partner_id)]"/>
                        <field name="ml_maturity_date"/>
                        <field name="date"/>
                        <field name="amount_currency" string="Amount" groups="base.group_multi_currency"/>
                        <field name="currency" groups="base.group_multi_currency"/>
                        <field name="name"/>
                        <field name="amount" sum="Total in Company Currency"/>
                        <field name="voucher_id"/>
                    </tree>
                </tree> -->
            </field>
        </record>
    </data>
</openerp>
