# because of company being argentina, template should be available
# not sure why, on transifex build the argentina chart is not installed and it
# raise an error
-
  !python {model: ir.module.module}: |
    self.pool['ir.module.module'].button_immediate_uninstall(cr, uid, [ref('base.module_l10n_ar_chart')])
-
  !python {model: account.chart.template, id: l10n_ar_chart.l10nar_ex_chart_template}: |
    self[0].with_context(with_bank_journal=True, create_point_of_sale_type=True).try_loading_for_current_company()