from odoo import models


class RestType(models.Model):
    """
    Parece que no es usado para nada pero es posible que si, tengo que
    analizarlo
    """

    _name = 'public_budget.rest_type'
    _description = 'rest_type'
